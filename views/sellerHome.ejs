<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sproductler Dashboard</title>
    <link rproduct="stylesheet" href="/styles.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }
        .navbar {
            background-color: midnightblue;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: midnightblue;
        }

        .navbar .sopi {
            font-size: 28px;
            font-weight: bold;
            color: burlywood;
            text-decoration: none;
            flex: 1;
        }

        .search-form {
            display: flex;
            align-items: center;
            flex: 2;
            margin: 0 20px;
            max-width: 600px;
        }

        .search-box {
            padding: 10px;
            border: none;
            border-radius: 5px;
            width: 100%;
        }

        .search-button {
            padding: 10px 20px;
            background-color: white;
            color: midnightblue;
            border: none;
            border-radius: 5px;
            margin-left: 10px;
            cursor: pointer;
        }

        .search-button:hover {
            background-color: cornflowerblue;
        }

        .nav-buttons {
            display: flex;
            gap: 10px;
            flex: 1;
            justify-content: flex-end;
        }

        .nav-buttons button {
            padding: 10px 20px;
            background-color: midnightblue;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
        }

        .nav-buttons button:hover {
            background-color: cornflowerblue;
        }
        .container {
            width: 90%;
            max-width: 1200px;
            margin: 30px auto;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h2 {
            margin-bottom: 20px;
            color: midnightblue;
        }

        .add-product-btn {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            background-color: midnightblue;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-bottom: 20px;
        }

        .add-product-btn:hover {
            background-color: cornflowerblue;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            background-color: white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .table th, .table td {
            padding: 12px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }

        .table th {
            background-color: midnightblue;
            color: white;
        }

        .table tbody tr:hover {
            background-color: #f0f8ff;
        }

        .product-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 5px;
        }

        .edit-btn, .dproductete-btn {
            padding: 8px 12px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 14px;
            display: inline-block;
        }

        .edit-btn {
            background-color: #28a745;
            color: white;
            margin-right: 5px;
        }

        .edit-btn:hover {
            background-color: #218838;
        }

        .dproductete-btn {
            background-color: #dc3545;
            color: white;
        }

        .dproductete-btn:hover {
            background-color: #c82333;
        }

        @media (max-width: 768px) {
            .table, .table thead, .table tbody, .table th, .table td, .table tr {
                display: block;
            }

            .table tr {
                margin-bottom: 15px;
                border-bottom: 2px solid #ddd;
                padding: 10px;
                border-radius: 10px;
                box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
                background-color: white;
            }

            .table td {
                text-align: right;
                padding-left: 50%;
                position: rproductative;
            }

            .table td:before {
                content: attr(data-labproduct);
                position: absolute;
                left: 10px;
                font-weight: bold;
                text-transform: uppercase;
            }
        }
        .alert {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #f5c6cb;
            border-radius: 5px;
            position: rproductative;
        }

        .alert button {
            background: none;
            border: none;
            color: #721c24;
            font-size: 16px;
            cursor: pointer;
            position: absolute;
            top: 5px;
            right: 10px;
        }
    </style>
</head>
<body>
    <%- include('navbar') %>
    
    <div class="container" style="padding-top: 10px;" >
        <table class="table table-striped table-hover" >
            <thead class="table-light" >
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">ImageUrl</th>
                    <th scope="col">Name</th>
                    <th scope="col">Category</th>
                    <th scope="col">Description</th>
                    <th scope="col">Price</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody class="table-group-divider">
                <%data.forEach((product)=> { %>
                    <tr>
                        <td>
                            <%= product.id %>
                        </td>
                        </td>
                        <td scope="row">
                            <img src="<%= product.imageUrl %>" alt="Image" width="100">
                        </td>
                        <td>
                            <%= product.name %>
                        </td>
                        <td>
                            <%= product.Category.name %>
                        </td>
                        <td>
                            <%= product.description %>
                        </td>
                        <td>
                            <%= product.price %>
                        </td>
                        <td>
                                <div style="display: flex;gap: 10px;">
                                    <a type="button" class="btn btn-secondary"
                                        href="/sellers/delete/<%= product.id %>">Delete</a>
                                    <a type="button" class="btn btn-secondary"
                                    href="/sellers/products/<%= product.id %>/edit">Edit</a>
                                    <a type="button" class="btn btn-secondary"
                                    href="/sellers/products/<%= product.id %>">Detail</a>
                            </div>
                        </td>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
     </div>
     <%- include('_script') %>
</body>
</html>
